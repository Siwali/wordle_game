/**
 * Game configuration constants
 *
 * This file contains all configuration values for the game.
 * Centralizing constants makes it easier to adjust game behavior.
 */

/**
 * Default maximum number of guesses allowed per round
 * Note: This can be overridden by difficulty settings
 */
export const MAX_GUESSES = 6

/**
 * localStorage key for persisting player level
 */
export const STORAGE_KEY = 'wordle_level'
